# Postgres credentials for knowai CLI (TOML format).
# Put this at ~/.knowai.config (global) or ./knowai.config (per-project).

[database]
host     = "localhost"
port     = 5432
user     = "knowai"
password = "knowai"
db       = "knowai"
schema   = "public"
